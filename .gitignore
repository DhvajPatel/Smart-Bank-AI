# Python
__pycache__/
*.pyc
*.pyo
.env

# Node
node_modules/
frontend/dist/

# Database — too large for Git (425 MB).
# Render rebuilds it automatically via: python load_data.py
backend/smartbank.db

# transactions_sample.csv is a small sample — not needed
ai/data/raw/transactions_sample.csv

# DO NOT ignore these — needed at deploy time:
#   ai/models/loan_ensemble.pkl   (1.65 MB — GBR model)
#   ai/models/loan_model.pt       (0.2  MB — MLP weights)
#   ai/models/loan_scaler.pkl     (tiny — StandardScaler)
#   ai/models/loan_metrics.json   (tiny — metrics)
#   ai/data/raw/customers.csv     (0.7  MB)
#   ai/data/raw/loans.csv         (0.3  MB)
#   ai/data/raw/accounts.csv      (0.6  MB)
#   ai/data/raw/credit_cards.csv  (0.3  MB)
#   ai/data/raw/product_interactions.csv (0.9 MB)
